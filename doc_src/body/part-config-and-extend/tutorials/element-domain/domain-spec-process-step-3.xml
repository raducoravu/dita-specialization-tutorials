<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic PUBLIC "-//OASIS//DTD DITA Topic//EN" "topic.dtd"> 
<topic id="domain-spec-step-three">
  <title>Element Domain Specialization Step 3: Declare The Module Entities File</title>
  <shortdesc>The module entities file declares entities that are used within document type shell DTDs to integrate the module into the shell.</shortdesc>
  <body>
    <p>The module entities file is named
                <filepath><varname>modulename</varname>Module.ent</filepath>. </p>
    <p>There are two types of entities: type-specific parameter entities that integrate the module's
            element types into the appropriate content models (phrase, keyword, dl, etc.), and a
            domain usage declaration text entity that goes in the topic's or map's domain use
            declaration attribute to indicate what domains are being used in a given topic or
            map.</p>
    <p>For the XML domain we only have element types specialized from <xmlelement>keyword</xmlelement>, so
            we only need to declare one type-specific parameter entity. The parameter entity is
            named <q><i>modulename</i>-d-<i>basetype</i>,</q> so in this case, the entity will be
            named <q>xml-d-keyword.</q></p>
  </body>
</topic>
